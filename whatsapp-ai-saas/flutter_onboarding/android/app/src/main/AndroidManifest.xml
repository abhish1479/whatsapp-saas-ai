<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.abhi.whatsappai.onboarding">

    <!-- Network permission -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- File picking via SAF generally doesn't need extra permissions -->

    <application
        android:label="WhatsApp AI Onboarding"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">

        <!-- For release, we usually do NOT allow cleartext HTTP.
             Use HTTPS for production API. Debug/Profile will override. -->

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">

            <!-- Flutter splash -->
            <meta-data
                android:name="io.flutter.embedding.android.SplashScreenDrawable"
                android:resource="@drawable/launch_background"/>

            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Flutter embedding v2 -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2"/>
    </application>
</manifest>
